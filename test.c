#include <stdio.h>

void main()
{
    int a,b,c,d,e,f,g,x;
    printf("Enter 7 numbers: ");
    scanf("%d%d%d%d%d%d%d",&a, &b, &c, &d, &e, &f, &g);
    x = ((a - b / c * d + e) * (f + g))
    printf("Solution is %d\n", x);
}
